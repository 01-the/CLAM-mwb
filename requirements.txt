<style>
  .mwb { font-family: inherit; color: #000; }
  .mwb-header { display: flex; justify-content: space-between; align-items: baseline;
    border-bottom: 2px solid #000; padding-bottom: 4px; margin-bottom: 6px; }
  .mwb-header .mwb-week { font-size: 16px; font-weight: 700; }
  .mwb-header .mwb-reading { font-size: 11px; font-style: italic; }
  .mwb-sections { display: flex; gap: 12px; }
  .mwb-section { flex: 1 1 0; min-width: 0; }
  .mwb-section h3 { font-size: 10px; text-transform: uppercase; letter-spacing: 0.02em;
    border-bottom: 1px solid #000; padding-bottom: 2px; margin: 0 0 4px 0; white-space: nowrap;
    overflow: hidden; text-overflow: ellipsis; }
  .mwb-item { font-size: 11px; line-height: 1.35; margin-bottom: 2px; }
  .mwb-num { font-weight: 700; }
  .mwb-item-time { font-size: 9px; }
</style>

<div class="layout">
  <div class="mwb">
    <div class="mwb-header">
      <span class="mwb-week">{{ week_label }}</span>
      <span class="mwb-reading">{{ bible_reading }} &middot; Song {{ opening_song }}</span>
    </div>
    <div class="mwb-sections">
      {% for sec in sections %}
        <div class="mwb-section">
          <h3>{{ sec.section }}</h3>
          {% for item in sec.items %}
            <div class="mwb-item"><span class="mwb-num">{{ item.num }}.</span> {{ item.title }} <span class="mwb-item-time">({{ item.time }})</span></div>
          {% endfor %}
        </div>
      {% endfor %}
    </div>
  </div>
</div>

<div class="title_bar">
  <img class="image" src="/images/plugins/trmnl--render.svg" alt="TRMNL Logo">
  <span class="title">Life &amp; Ministry Meeting</span>
  <span class="instance">{{ week_label }}</span>
</div>
